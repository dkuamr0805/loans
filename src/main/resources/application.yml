server:
 port: 8090

spring:
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: update
 datasource:
  url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/loans
  username: root
  password: root
  driver-class-name: com.mysql.cj.jdbc.Driver
